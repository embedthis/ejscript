<script>
/*
    //  Server-sent events
    var source = new EventSource('/Dash/commet');
    source.onopen = function (event) {
        console.debug("OPEN: " + event);
    };
    source.onerror = function (event) {
        console.debug("ERROR: " + event);
    };
    source.onmessage = function (event) {
        console.debug("DATA: " + event.data);
    };
*/

/*  
    // Web Sockets
    var ws = new WebSocket("ws://127.0.0.1:4000/ws");
    ws.onopen = function (e) { alert("OPEN"); }
    ws.onmessage = function (e) { alert("DATA" + e.data); }
    ws.onclose = function (e) { alert("CLOSE"); }
    ws.onerror = function (e) { alert("ERROR"); }

    ws.send("GET /Dash/commet HTTP/1.1\r\n\r\n");
    // ws.close();
*/
</script>


