/*
    unix.me -- MakeMe file for ejs.unix
 */

Me.load({

    targets: {
        'ejs.unix.mod': {
            path: '${BIN}/ejs.unix.mod',
            files: [ '*.es' ],
            precious: true,
            build: `
                run('"${LBIN}/ejsc" --out "${BIN}/ejs.unix.mod" ${settings.esflags} --optimize 9 ${FILES}')
            `,
            message: 'Compile: ejs.unix.mod',
            depends: [ 'ejsc', 'ejs.mod' ],
        }
	}
})
