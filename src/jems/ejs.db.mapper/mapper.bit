/*
    mapper.bit -- Bit File for ejs.db.mapper
 */

Bit.load({

    targets: {
        'ejs.db.mapper.mod': {
            path: '${LIB}/ejs.db.mapper.mod',
            files: [ '*.es' ],
            build: "
                trace('Compile', 'ejs.db.mapper.mod')
                run('ejsc --out ${LIB}/ejs.db.mapper.mod ${settings.esflags} --optimize 9 ' + Path('.').glob('*.es').sort().join(' '))
            ",
            depends: [ 'ejsc', 'ejsmod', 'ejs.mod' ],
        },
	},
})
