/*
    rpmbuild.bit - Fedora package maker for Bit
 */

if (bit.platform.os == 'linux' && bit.platform.dist == 'fedora') {
    let rpmbuild = probe('rpmbuild', {fullpath: true})
    Bit.load({packs: { rpmbuild: {path: rpmbuild}}})
}


