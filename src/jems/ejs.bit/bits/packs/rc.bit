/*
    rc.bit - Resource compiler package for Bit
 */
pack('rc', 'Windows Resource Compiler')

let rc = (Config.OS != 'WIN') ? 'rc' : probe('rc.exe', {fullpath: true, search: bit.packs.compiler.search})
Bit.load({packs: { rc: { path: rc }}})
