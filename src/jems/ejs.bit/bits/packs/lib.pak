/*
    lib.pak - Library package for Bit
 */

pack('lib', 'Library Archive Tool')
let name = (bit.platform.os == 'windows') ? 'lib' : 'ar'
Bit.load({packs: { lib: { path: probe(bit.packs.lib.path || name, {fullpath: true})}}})

