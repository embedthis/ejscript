/*
    Test initialization
 */
require ejs.unix

const PORT  = test.config["http_port"]
const HTTP  = "127.0.0.1:" + PORT
const HTTPS  = "127.0.0.1:" + test.config["ssl_port"]

startService("server.es", HTTP, "/index.html")

share("https", HTTPS)
share("port", PORT)
