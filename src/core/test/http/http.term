require ejs.unix

/*
     Stop the web server
 */
pidfile = session["webPidFile"]
if (pidfile && exists(pidfile)) {
    path = Path(pidfile)
    pid = path.readString()
    kill(pid)
    path.remove()
}
