<% 
    /*
        Async wait for 5 seconds
     */
    request.dontFinalize()
    Timer(5000, function () {
        try {
            request.finalize(true)
        } catch {}
    }, request).start()
%>
