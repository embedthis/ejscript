<% 
    /*
        Async wait for 5 seconds
     */
    request.dontAutoFinalize()
    Timer(5000, function () {
print("INSIDE TIMER")
        try {
            request.finalize()
print("AFTER FINALIZE TIMER")
        } catch {}
    }, request).start()
%>
