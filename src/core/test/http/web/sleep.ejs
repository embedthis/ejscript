<% 
    /*
        Async wait for 5 seconds
     */
    request.dontAutoFinalize()
    Timer(5000, function () {
        try {
            request.finalize()
        } catch {}
    }, request).start()
%>
