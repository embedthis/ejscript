

dest = {
    session : {
        timeout: 4000,
        enable: true
    },
    top: 7,
}

src = {
    top: 8,
    session: {
        big: true,
        timeout: 5000,
    },
}

d = dest.clone()
blend(d, src, false)
dump(d)
print(d.session.timeout)
assert(d.session.timeout == 4000)

